<body>
<nav>
    <a href="/index" class="nav-brand">Cap<em>trieve</em></a>
    <ul class="nav-links">
        {% for item in site.nav %}
        <li><a href="{{ item.href }}">{{ item.label }}</a></li>
        {% endfor %}
    </ul>
    <a href="{{ site.navCta.href }}" class="nav-cta">{{ site.navCta.label }}</a>
</nav>
